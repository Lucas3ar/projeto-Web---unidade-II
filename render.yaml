services:
  - type: web
    name: retro-games-store
    env: docker
    plan: free
    autoDeploy: true
    healthCheckPath: /
    envVars:
      - key: DB_HOST
        fromDatabase:
          name: retro-games-db
          property: host
      - key: DB_PORT
        fromDatabase:
          name: retro-games-db
          property: port
      - key: DB_NAME
        fromDatabase:
          name: retro-games-db
          property: database
      - key: DB_USER
        fromDatabase:
          name: retro-games-db
          property: user
      - key: DB_PASSWORD
        fromDatabase:
          name: retro-games-db
          property: password
      - key: PORT
        value: 8080

databases:
  - name: retro-games-db
    plan: free
    databaseName: retrodb
    user: retro_user
